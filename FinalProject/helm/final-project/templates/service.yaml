apiVersion: v1
kind: Service
metadata:
  name: {{ include "final-project.fullname" . }}
  labels:
    {{- include "final-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "final-project.selectorLabels" . | nindent 4 }}
