apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "final-project.fullname" . }}-test-connection"
  labels:
    {{- include "final-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "final-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
